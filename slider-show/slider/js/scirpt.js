new slider({
    el : document.querySelector('#sliders1'),
    sliderClass : 'slider',
    currentSlider : (slider) => {
        // console.log(slider);
    },
    auto : 5000
})


new slider({
    el : document.querySelector('#sliders2'),
    sliderClass : 'slider',
    currentSlider : (slider) => {
        // console.log(slider);
    },
    auto : 1000
})